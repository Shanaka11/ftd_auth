from setuptools import setup

setup(    
        install_requires=[
        'djangorestframework',
        'djangorestframework-simplejwt',
        'django-cors-headers',
        'django-environ'
        ]
    )